---
version: 2
updates:
  # Keep the reusable-workflow refs (nkg/github-actions@v2) and any actions
  # used in .github/workflows up to date.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
